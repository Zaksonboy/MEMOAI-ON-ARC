# momoAI — USDC Payment Assistant on Arc

**AI-written, permanently on-chain payment memos for USDC transfers on Arc Mainnet.**

momoAI lets you send USDC with a clear, professional memo attached to every transaction — generated by AI and recorded permanently on-chain through Arc's native Memo contract. No off-chain database, no lost context.

🔗 **Live app:** (https://memoarc.xyz)
💻 **Repo:** [github.com/Zaksonboy/MEMOAI-ON-ARC](https://github.com/Zaksonboy/MEMOAI-ON-ARC)

---

## What it does

1. Connect your wallet — the app detects and switches to Arc Mainnet automatically.
2. Enter a recipient address, an amount, and a short description of what the payment is for.
3. Tap **Generate** — AI turns your description into a clean, professional memo.
4. The app builds a full structured record: sender, recipient, amount, reason, date, and a sequential invoice reference (e.g. `momoai-2026-0001`).
5. Tap **Send Payment** — the app calls Arc's Memo contract, which wraps a real USDC transfer and attaches the memo as part of the same on-chain transaction.
6. Get a direct link to the confirmed transaction on Arc's explorer, and see it saved in your local payment history.

## Additional features

- **Look Up** — search for any past payment directly on-chain using its invoice reference number. The app queries Arc's Memo contract events and reconciles the result — no off-chain database involved, and no need for the original transaction hash.
- **Contacts** — save a name against a wallet address for faster future payments. Stored privately on your own device only; never shared or visible to anyone else.

## Why it matters

Standard token transfers only show an amount and two addresses — no context. momoAI attaches the *reason* for a payment directly to the transaction itself, using Arc's purpose-built Memo contract, so the explanation is permanent, public, and doesn't depend on any external database staying online. The invoice reference system means any payment can later be independently verified and reconciled by anyone, using nothing but Arc's own chain data.

## Tech stack

| Layer | Technology |
|---|---|
| Blockchain | Arc Mainnet (Chain ID `5042`) |
| Wallet & transactions | ethers.js v6 |
| AI memo generation | Google Gemini API |
| On-chain memo storage | Arc's predeployed Memo contract |
| Backend | Vercel serverless functions |
| Hosting | Vercel |

## Contracts used

momoAI doesn't deploy its own contract — it calls two contracts already provided by Arc:

- **USDC (system contract):** `0x3600000000000000000000000000000000000000`
- **Memo contract:** `0x5294E9927c3306DcBaDb03fe70b92e01cCede505`

The Memo contract wraps a standard USDC `transfer()` call and attaches your memo in the same transaction, while preserving your wallet as the true sender.

## Network details

| | |
|---|---|
| Network | Arc Mainnet |
| Chain ID | `5042` |
| RPC | `https://rpc.mainnet.arc.io` |
| Explorer | `https://explorer.arc.io` |

## Project structure

```
├── index.html          → Landing page
├── app.html             → The payment app (wallet, memo generator, send flow, Look Up, Contacts)
├── script.js             → Wallet connection, memo building, on-chain transaction logic, invoice lookup
├── style.css             → App styling
├── landing.css           → Landing page styling
├── api/
│   └── generate-memo.js  → Serverless function bridging the app to Gemini AI
├── images/                → Logos, favicons, and social preview assets
└── vercel.json            → Deployment configuration
```

## Running it yourself

1. Clone the repo
2. Set the following environment variable in your Vercel project settings:
   - `GEMINI_API_KEY` — a free API key from [Google AI Studio](https://aistudio.google.com)
3. Deploy to Vercel — no build step required, it's a static frontend with one serverless function
